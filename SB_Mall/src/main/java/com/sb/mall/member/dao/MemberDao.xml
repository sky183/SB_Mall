<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.sb.mall.member.dao.MemberDao">
 	
 	<insert id="insert"
 		parameterType="com.sb.mall.member.model.MemberInfo"
 		useGeneratedKeys="true" keyProperty="userSeq">
 		insert into MemberInfo (userId,userPw,userName,address1,address2,zipCode,phone,birth,gender)
 		values (#{userId},#{userPw},#{userName},#{address1},#{address2},#{zipCode},#{phone},#{birth},#{gender})
 	</insert>

	
	<select id="selectById"
		resultType="com.sb.mall.member.model.MemberInfo">
		select * from MemberInfo where userid = #{userId}
	</select>

	<select id="selectAll"
		resultType="com.sb.mall.member.model.MemberInfo">
		select * from MemberInfo
	</select>

	<update id="update"
		parameterType="com.sb.mall.member.model.MemberInfo">
		update MemberInfo 
		<set>
		<if test="${userPw!=null}">
		userpw=#{userPw},
		</if>
		username=#{userName}, address1=#{address1}, address2=#{address2},zipCode=#{zipCode}, phone=#{phone},
		gradenum=#{gradeNum}, point=#{point}, useramount=#{userAmount} 
		</set>
		where userid=#{userId}
	</update>

	<delete id="delete" parameterType="String">
		delete from MemberInfo where
		userid = #{userId}
	</delete>

	<select id="selectCount" resultType="int">
		select count(*) from
		MemberInfo
	</select>

	<select id="selectList"
		resultType="com.sb.mall.member.model.MemberInfo">
		select * from
		MemberInfo order by userSeq desc limit
		#{param1},
		#{param2}
	</select>

	<select id="selectTodayList"
		resultType="com.sb.mall.member.model.MemberInfo">
		select * from
		MemberInfo where regDate = curDate() order by userSeq desc limit
		#{param1},
		#{param2}
	</select>

	<!--파라미터 타입은 입력값 ,리설트타입은 출력값 -->
	<select id="find_id" parameterType="String" resultType="com.sb.mall.member.model.MemberInfo">

		<!-- select userId from MemberInfo where username = #{userName} and phone 
			= #{phone} -->
		<!--여러개를 받아올때 param1 -->
		select * from MemberInfo where username = #{param1} and phone =
		#{param2}

	</select>
	
	<!--비밀번호 찾기 -->
	<select id="find_pw" parameterType="String" resultType="String">
		select
		userPw from MemberInfo where userId = #{param1} and userName =
		#{param2}

	</select>



</mapper>