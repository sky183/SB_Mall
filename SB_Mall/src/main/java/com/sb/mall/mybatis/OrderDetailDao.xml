<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.sb.mall.dao.OrderDetailDao">
 
 	<insert id="insert"	parameterType="com.sb.mall.model.OrderDetail">
 	</insert>
 	
	<delete id="delete" parameterType="int">
	</delete>

	<select id="selectAll" resultType="List">
		select * from OrderDetail where status<![CDATA[<>]]>4
	</select>
	
	<select id="selectColumn" resultType="List" parameterType="map">
	</select>
	
	<select id="selectJoin" resultType="List" parameterType="map">
	</select>
	
	<select id="selectMemberOrder" resultType="List">
	</select>
	
	<select id="selectOrderDetail" resultType="com.sb.mall.model.OrderDetail">
	</select>
	
	
 </mapper>