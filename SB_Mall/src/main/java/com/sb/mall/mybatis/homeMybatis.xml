<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sb.mall.dao.HomeDao">
	<select id="selectDailyDeal" resultType="map">
		select * from SaleBoard s, product p where s.productSeq=p.productSeq order by viewSeq Desc limit 0,5
	</select>
	
	<select id="selectNewProduct" resultType="com.sb.mall.model.MemberInfo">
		select * from SaleBoard s, product p where s.productSeq=p.productSeq order by writeSeq Desc limit 0,8
	</select>
</mapper>