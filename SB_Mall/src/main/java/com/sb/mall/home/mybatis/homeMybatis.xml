<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sb.mall.home.dao.HomeDao">
	<select id="selectDailyDeal" resultType="map">
		select * from SalesBoard s, Product p where s.productSeq=p.productSeq order by viewSeq Desc limit 0,5
	</select>
	
	<select id="selectNewProduct" resultType="map">
		select * from SalesBoard s, Product p where s.productSeq=p.productSeq order by writeDate Desc limit 0,8
	</select>
</mapper>